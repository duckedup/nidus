//! nidus-bench — cross-engine exact-KNN performance-parity benchmark.
//!
//! Harness, dataset generation, metrics, and engine adapters are added in the
//! follow-up tasks (nidus-1ox onward). This stub exists so the workspace resolves
//! and the build-path isolation (heavy deps stay off nidus's default build) can be
//! verified now.

fn main() {
    println!("nidus-bench: harness not yet implemented (see bd epic nidus-rq9)");
}

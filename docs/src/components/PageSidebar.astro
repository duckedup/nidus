---
// Intentionally empty. nidus docs use a single left sidebar, so the right-hand
// "On this page" table of contents is removed. Emptying PageSidebar (rather
// than just hiding it in CSS) means the column reserves no space and the
// reading content reflows to a clean, centred measure.
---

#!/usr/bin/env sh
# Prefer bun, fall back to node. Uses only APIs both runtimes share.
set -e
dir=$(CDPATH= cd -- "$(dirname -- "$0")/../lib" && pwd)
if command -v bun >/dev/null 2>&1; then
  exec bun "$dir/spec.mjs" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node "$dir/spec.mjs" "$@"
else
  echo "spec needs bun or node on PATH" >&2
  exit 127
fi

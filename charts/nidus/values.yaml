# Default values for the nidus chart.
# See the chart README for a walkthrough; every nidus.* value maps to a NIDUS_*
# environment variable consumed by `nidus serve`.

# ── Image ─────────────────────────────────────────────────────────────────────
image:
  repository: duckedup/nidus
  # Tag defaults to the chart's appVersion when left empty.
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# ── nidus store configuration (→ NIDUS_* env) ──────────────────────────────────
# The image runs with NIDUS_REQUIRE_REMOTE=true: it refuses to start unless backed
# by a *shared, non-local* store. So `dim`, `persistence`, and `memory` are REQUIRED;
# the chart fails fast at render time if any is missing.
nidus:
  # Embedding dimension — pinned when the store is first created. REQUIRED.
  dim: 0

  # Durable bytes — an object store. REQUIRED.
  #   s3://<bucket>[/<prefix>]   or   gs://<bucket>[/<prefix>]
  persistence: ""

  # Shared in-RAM working set — a Redis-family tier. REQUIRED (set `memory` OR
  # `memorySecret`). Credentials go in the URL userinfo and TLS uses the rediss://
  # scheme: redis://host:6379, rediss://user:pass@host:6380 (also valkey://, keydb://,
  # dragonfly://). Append ?cluster=true for a Redis/Valkey Cluster — one seed host, or
  # several comma-separated (redis://a,b,c:6379?cluster=true); the rest of the topology is
  # discovered. When the URL carries a password, prefer `memorySecret` below so it never
  # lands in the Deployment manifest in plain text.
  memory: ""
  # Source the full NIDUS_MEMORY URL from a Secret instead of `memory` above. Use this
  # for an authenticated Redis so the password stays out of rendered manifests/values.
  memorySecret:
    name: ""
    key: NIDUS_MEMORY

  # Run as cooperating instances over the SAME shared store (SPEC §14.6). Required for
  # standby promotion; needs both `persistence` (object store) and `memory` (Redis-family)
  # to be shared, and is refused otherwise. Not a managed cluster — no coordinator,
  # replication, or rebalancing: exactly one instance holds a fenced writer lease, and the
  # object store's conditional writes are what make that safe.
  cluster: false

  # Stay up and wait for the writer handle instead of exiting when another instance holds
  # it — this is what makes a replica a HOT STANDBY rather than a crash-looping pod. A
  # standby reports NOT ready (so the Service skips it) until it is promoted, which happens
  # within about `lockTtl` of the active writer dying. Pair with replicaCount > 1.
  waitForLease: false

  # Seconds before another instance may reclaim a stale writer lease (default 60 in nidus).
  # This is also the standby promotion window, so it is the main failover-latency knob:
  # lower means faster takeover, but too low risks fencing a writer that is merely slow.
  lockTtl: 0

  # Read-only instances: refresh every N seconds so they stay current without a sidecar
  # calling POST /refresh (0 = never, the default), and fail readiness if they ever fall
  # more than maxStaleness seconds behind (0 = no bound). Set both on reader replicas:
  # the interval keeps them fresh, the bound takes them out of the Service if it stops.
  refreshInterval: 0
  maxStaleness: 0

  # Distance metric: "" (cosine, the default) | cosine | euclidean | dot.
  # Only honored when the store is first created.
  distance: ""

  # Max request/upsert body size in bytes. "" leaves the server default (256 MiB).
  maxBodyBytes: ""

  # ── Backpressure ─────────────────────────────────────────────────────────────
  # Cap on store-touching requests in flight; past it, requests are SHED with a
  # retryable 503 + Retry-After rather than queued. "" leaves the server default,
  # which is auto: 8x CPU cores, floored at 64. Set it explicitly when you have set
  # a CPU limit, since `available_parallelism` sees the node's cores, not the pod's
  # quota — auto will overshoot on a small `resources.limits.cpu`.
  # /health, /ready and /metrics are never shed.
  maxConcurrentRequests: ""
  # Per-request deadlines in seconds ("" = server default; "0" = no deadline).
  # A read is milliseconds, a large upsert legitimately runs for minutes, which is
  # why they are separate knobs. Note a timeout frees the CLIENT, not the CPU: a
  # scan already running finishes anyway.
  readTimeout: ""    # default 30
  writeTimeout: ""   # default 600
  # Abandon a request body that stops delivering data for this many seconds ("" =
  # server default of 15; "0" = no bound). An IDLE bound, so a large upsert over a
  # slow link is never cut off. It exists because a request holds a concurrency
  # permit while its body arrives: without it a client that sends headers and then
  # goes silent pins one. Real slow-client defence belongs on the ingress.
  bodyIdleTimeout: ""
  # Log level: error | warn | info (default) | debug | trace | off. Logs are one
  # key=value line per event on stderr, with a per-request id. `error` silences the
  # per-request access log; `debug` turns on writer-lease tracing.
  logLevel: ""

  # Approximate-nearest-neighbour index: "" (exact brute force) | hnsw | ivf.
  ann: ""
  # Optional ANN tuning — each non-empty key becomes a NIDUS_ANN_* env var.
  # m, efConstruction, efSearch, nLists, nProbe, overscan, seed
  annParams: {}

# ── Bearer-token auth ───────────────────────────────────────────────────────────
# Strongly recommended whenever the Service is reachable beyond localhost. When
# enabled, every request except the probes (GET /health, /ready, /metrics) needs
# `Authorization: Bearer <token>`.
#
# READ THIS BEFORE EXPOSING THE SERVICE. nidus speaks PLAIN HTTP by design — there
# is no in-process TLS, and that is a decision, not a gap: TLS belongs at the
# ingress/sidecar/mesh you are already running. Consequences:
#
#   * `auth.token` authenticates a caller; it does NOT confer confidentiality. Over
#     plain HTTP the token, every vector, every id, and every metadata value cross
#     the network in cleartext on every request.
#   * The chart's ClusterIP Service is in-cluster only, so pod-to-pod traffic is as
#     private as your CNI makes it. The moment you expose it — `ingress.enabled`, a
#     LoadBalancer Service, or a mesh gateway — terminate TLS there. Set
#     `ingress.tls` (below) with a cert-manager or hand-managed Secret; a mesh with
#     strict mTLS covers the in-cluster leg too.
#   * Rate limiting belongs on the ingress, not in nidus. nidus bounds total
#     in-flight work (see nidus.maxConcurrentRequests) but does nothing per-client.
#   * The token model is one static shared secret: no rotation without a restart,
#     no per-collection scoping, no user identity. Issue scoped or rotatable
#     credentials at the proxy if you need them.
#
# nidus logs a warning at startup when it binds a non-loopback address (which the
# container always does) — with a token, that the credential is in cleartext;
# without one, that the store is open to anyone who can reach it. It warns and
# starts; it never refuses, because refusing would break the proxy-terminated
# architecture recommended here.
auth:
  enabled: false
  # Provide the token inline (stored in a chart-managed Secret)…
  token: ""
  # …or point at a key in a Secret you manage yourself (takes precedence).
  existingSecret: ""
  existingSecretKey: token

# ── Backend credentials (→ env) ─────────────────────────────────────────────────
# Two ways to authenticate:
#  (a) Keyless (recommended on EKS/GKE) — leave `credentials` empty and bind the
#      ServiceAccount to a cloud role via `serviceAccount.annotations`:
#        EKS/IRSA: eks.amazonaws.com/role-arn: arn:aws:iam::<acct>:role/<role>
#        GKE WI:   iam.gke.io/gcp-service-account: <sa>@<project>.iam.gserviceaccount.com
#      nidus picks up the injected web-identity token (S3) or metadata server (GCS) and
#      refreshes the temporary credentials automatically. ECS/EC2 instance roles also work.
#  (b) Static keys, supplied here:
#        S3 (s3://):  AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY (+ optional AWS_SESSION_TOKEN,
#                     AWS_REGION, AWS_ENDPOINT_URL for R2/MinIO).
#        GCS (gs://): a service-account key as GOOGLE_APPLICATION_CREDENTIALS_JSON (key JSON
#                     inline). Put it in an existing Secret and list it below.
credentials:
  # Inline key/value pairs are written to a chart-managed Secret and loaded via
  # envFrom. e.g.
  #   AWS_ACCESS_KEY_ID: "AKIA…"
  #   AWS_SECRET_ACCESS_KEY: "…"
  #   AWS_REGION: "us-east-1"
  #   AWS_ENDPOINT_URL: "http://minio:9000"   # for MinIO / non-AWS S3
  inline: {}
  # Existing Secrets to load wholesale via envFrom — the recommended path for real
  # secrets (SealedSecrets, External Secrets Operator, or a hand-made Secret). For GCS,
  # the Secret holds a GOOGLE_APPLICATION_CREDENTIALS_JSON key with the SA key JSON.
  existingSecrets: []

# Self-managed AWS web identity (IRSA-style) for S3 on clusters WITHOUT the EKS pod-identity
# webhook — e.g. self-hosted Kubernetes federated to AWS IAM through an OIDC provider. The
# chart projects a ServiceAccount token and points nidus at it (sets AWS_ROLE_ARN and
# AWS_WEB_IDENTITY_TOKEN_FILE), so S3 authenticates with no static keys. On EKS leave this
# disabled — the webhook injects the equivalent automatically from the SA annotation.
awsWebIdentity:
  enabled: false
  # The IAM role to assume (→ AWS_ROLE_ARN). Required when enabled.
  roleArn: ""
  # Projected-token audience; must match the audience configured on the IAM OIDC provider.
  audience: sts.amazonaws.com
  # Token lifetime before kubelet rotates the projected file (nidus re-reads it each refresh).
  expirationSeconds: 3600

# Escape hatches for anything not modeled above.
extraEnv: []
extraEnvFrom: []

# ── Workload ────────────────────────────────────────────────────────────────────
# `nidus serve` is a SINGLE writer: exactly one instance holds the writer handle on the
# shared backend at a time. What the extra replicas do depends on how you configure them:
#
#   * DEFAULT (replicaCount: 1) — one writer, no standby. Simplest, and correct.
#   * HOT STANDBY — set replicaCount > 1 AND nidus.waitForLease: true. The losers stay up
#     waiting for the writer handle instead of exiting, and one is promoted within about
#     nidus.lockTtl of the writer dying. They report NOT ready while waiting, so the
#     Service routes only to the active writer.
#   * replicaCount > 1 WITHOUT waitForLease — don't. The extra pods lose the lock race and
#     crash-loop, which is an alert, not a design.
#
# Standby promotion needs cluster mode (nidus.cluster: true), which in turn needs a shared
# object store AND a shared memory tier — a local-disk store is single-node by definition.
replicaCount: 1

# Recreate (not RollingUpdate): the old writer must terminate — releasing its lock on
# SIGTERM — before the replacement starts, or the new pod hits a held-lock error.
#
# With `waitForLease: true` a RollingUpdate becomes viable, since the incoming pod waits
# for the handle rather than failing: the old writer releases on SIGTERM and the new one is
# promoted. Recreate stays the default because it is correct in every configuration, and
# because a rolling update trades a brief write outage for one that is briefer but harder
# to reason about. Change it deliberately, not by default.
updateStrategy:
  type: Recreate

# Give the writer time to flush and release its lock on SIGTERM before SIGKILL.
terminationGracePeriodSeconds: 30

resources: {}
  # limits:
  #   cpu: "2"
  #   memory: 2Gi
  # requests:
  #   cpu: 500m
  #   memory: 512Mi

# The image already runs as nonroot (uid 65532) and writes nothing to local disk.
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  fsGroup: 65532
securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

# Probes hit the unauthenticated /health endpoint.
livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
  timeoutSeconds: 2
  failureThreshold: 3
# Readiness is /ready, NOT /health: the two differ for a standby. /health means "the
# process is alive" and answers even while an instance waits for the writer handle —
# probing it for readiness would put a standby into the Service and have it answer 503 to
# real traffic. /ready means "this instance has a store open and can serve".
readinessProbe:
  httpGet:
    path: /ready
    port: http
  initialDelaySeconds: 3
  periodSeconds: 5
  timeoutSeconds: 2
  failureThreshold: 3

nodeSelector: {}
tolerations: []
affinity: {}
podAnnotations: {}
podLabels: {}

# ── Service ───────────────────────────────────────────────────────────────────
service:
  type: ClusterIP
  port: 7700
  annotations: {}

# ── Ingress ───────────────────────────────────────────────────────────────────
# This is where TLS terminates. nidus serves plain HTTP (see the auth section
# above), so an Ingress with an empty `tls:` publishes the bearer token and every
# vector in cleartext to anything on the path. Populate `tls` whenever `enabled`.
#
# Two more things worth setting here rather than in nidus:
#   * A proxy body-size limit matching nidus.maxBodyBytes — most ingress controllers
#     default to 1 MiB and will reject an upsert long before nidus sees it
#     (nginx: nginx.ingress.kubernetes.io/proxy-body-size).
#   * A proxy read timeout at least as long as nidus.writeTimeout, or the proxy will
#     cut off a legitimate large upsert mid-batch
#     (nginx: nginx.ingress.kubernetes.io/proxy-read-timeout).
#
# Keep /metrics off any public host: it exposes traffic shape (never collection
# names or data), and it is deliberately unauthenticated so a scraper is not
# reported as down.
ingress:
  enabled: false
  className: ""
  annotations: {}
    # nginx.ingress.kubernetes.io/proxy-body-size: "256m"
    # nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
  hosts:
    - host: nidus.local
      paths:
        - path: /
          pathType: Prefix
  # e.g.
  #   - hosts: [nidus.local]
  #     secretName: nidus-tls    # cert-manager, or a Secret you manage
  tls: []

# ── ServiceAccount ──────────────────────────────────────────────────────────────
serviceAccount:
  create: true
  name: ""
  annotations: {}
